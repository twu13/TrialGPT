# Application configuration
# Copy to .env and fill in values as needed.

# OpenAI API key (required for LLM-powered parsing/answering)
OPENAI_API_KEY=

# Qdrant connection (local by default)
QDRANT_URL=http://localhost:6333
COLLECTION_NAME=clinical_trials

# ClinicalTrials.gov API (v2)
# Use the v2 base; client targets /studies with pageToken pagination.
CTGOV_API_BASE=https://clinicaltrials.gov/api/v2

# Ingestion window (ISO dates; empty means all available)
DATA_START_DATE=2024-01-01
DATA_END_DATE=

# Optional: Streamlit/App settings
# APP_PORT=8501
# APP_ENV=dev
